server:
  port: 8899

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/zantt
    username: root
    password: dnjem!1
  jpa:
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update